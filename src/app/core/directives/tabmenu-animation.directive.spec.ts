import { TabmenuAnimationDirective } from './tabmenu-animation.directive';

describe('TabmenuAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new TabmenuAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
